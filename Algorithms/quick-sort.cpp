#include <bits/stdc++.h>
void swap(int &a, int &b){
    int temp = a;
    a = b;
    b = temp;
}
int makePartition(int A[], int start, int end){
    int pivot = A[end];
    int pIndex = start;
    for(int i=start;i<end;i++){
        if(a[i]>=)
    }
}
int main(){
    int a=10, b=1;
    swap(a,b);
    std::cout<<a<<b;
}