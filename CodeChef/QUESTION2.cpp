#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;

#define CIN_START                     
  do                                  
  {                                   
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL);                    
  } while (0)

// ans
struct myStruct
{
  char name[20];
  int acc;
  struct myStruct *next;
};

int main()
{
  CIN_START;
  struct myStruct a;
  cout << sizeof(a.next);
  int ✓ = 7;
  cout<<✓;
  return 0;
}
